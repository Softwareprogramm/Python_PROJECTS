def convert(miles):
    return 1.6 * miles

while True:
    user = input("enter a number: ")
    if user.isdigit():
        user = int(user)
        result = convert(user)
        print(result)
        break
    else:
        print("Please enter a number. Try again")
